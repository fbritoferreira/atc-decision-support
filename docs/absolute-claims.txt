26 absolute claims, pinned. Regenerate with scripts/absolute-claims.mjs.

../web/src/papers/atc-decision-support.md: Advisory when wet, warning when snow or ice | Surface state is not carried by ADS-B or METAR in a form this ingests, so it is set per scenario and never fires on live data.
../web/src/papers/atc-decision-support.md: And the proximity detector excluded aircraft in the `departed` phase, which live ingest assigns to every departure it classifies, so a whole traffic class was unreachable live while the scenarios that would expose it encoded the phase differently; that exclusion was removed once the sampling window it would have disturbed had closed.
../web/src/papers/atc-decision-support.md: Each aircraft is measured against its stream's first member now, which bounds the width by construction.
../web/src/papers/atc-decision-support.md: KDEN sits above that cutoff, so an aircraft standing on its runway is already excluded and attribution never fires there at all, which leaves the runway-conflict detector and the parallel-runway demotion inert at that field.
../web/src/papers/atc-decision-support.md: Read at the time as two unreachable detectors, then as four; three are, gate, runway identity and runway surface, and Section 6.7 says why.
../web/src/papers/atc-decision-support.md: Six places recorded the doctrine as unreachable on live data on the strength of the first half, and the module that performs the smoothing recorded nineteen criticals measured in one Los Angeles window.
../web/src/papers/atc-decision-support.md: That is the scope it was built for, since its purpose is airborne proximity triage and a ground departure gains nothing from a runway label, and it leaves the runway-identity doctrine unreachable on live data exactly as Section 6.1 and the phase-vocabulary audit describe, so the false critical cannot arise in production.
../web/src/papers/atc-decision-support.md: The ninth, American 11 on 11 September 2001, is included as a documented blind spot: its hijack signatures are invisible to this architecture by construction, and a test enforces that silence.
../web/src/papers/atc-decision-support.md: This thesis has proposed a multi-detector decision-support architecture for air traffic control and exercised it against nine publicly-documented historical incidents, eight of them detectable by construction.
src/sim/live-adsb.ts: 0; // Reached only for a contact isArrival or isDeparture accepted, and both // now decline a record with no track, so this is defined by construction.
src/sim/live-adsb.ts: 0` on lat and lon in these two classifiers cannot fire.
src/sim/live-adsb.ts: The only caller // queries the radius endpoint /v2/lat/{lat}/lon/{lon}/dist/{range}, which // reports a distance per aircraft because that is what a radius query is // for, so this branch is unreachable on that path rather than merely // believed to be, and declining costs nothing measured.
src/sim/live-weather.ts: Left as "none" // rather than marked, because the type has no way to say "unknown" and // adding one to serve an unreachable branch buys nothing; the reasoning is // here so the next person does not have to redo it.
src/sim/rules.ts: 20 degrees is tighter than the 30 used for final alignment and for course // attribution, and it can be, because a departure on its roll is aligned with // a strip by construction rather than intercepting one.
src/sim/rules.ts: Criticals are unreachable, and by the ETA cap rather than by this * construction.
src/sim/rules.ts: Only `dry` and `wet` occur anywhere in the registry or the corpus, so the // snow and ice branches are reachable by construction and unexercised in // fact.
src/sim/rules.ts: The detector is guarded against that by construction // rather than by the number, since it fires only when an aircraft is OUTSIDE // tolerance of its assigned runway and inside it for a different one, and that ordering was described here as what makes 20 safe rather than the // value itself.
the long-form paper: Attribution is therefore scoped to airborne departures, which is the scope it was built for and which leaves the wrong-runway doctrine unreachable on live data exactly as before, so the false critical cannot occur in production.
the long-form paper: Attribution reached airborne departures on 26 August 2026, so the runways now exist; the identity doctrine still cannot fire, because the departures it reads are the ones at altitude zero, which ingest labels queued and does not attribute.
the long-form paper: Criticals are unreachable now, through a sixty-minute cap on the ETA rather than through the synthesis, and a test pins the boundary.
the long-form paper: Six places recorded the doctrine as unreachable live on the strength of the first half, while the module doing the smoothing recorded nineteen criticals measured in one Los Angeles window.
the long-form paper: That limit is also why the runway-identity doctrine cannot fire on live data, and this was its second consequence.
the long-form paper: The intervals are disjoint and the unreachable group is the higher one, which is the same result in the same direction as the window above.
the long-form paper: The ninth, American 11 on 11 September 2001, is treated separately below, because the architecture provably cannot detect it.
the long-form paper: The unreachable rate is 2.7 per cent here against 4.9 in the first window, and those two intervals overlap between 3.6 and 4.2, so the difference between the windows is not itself a finding; it is what two samples of a few hundred pairs each look like when they agree on a direction.
the long-form paper: This sentence said by construction until 2026-09-01, which was true of the windows measured here only because their weather was clear and dry throughout.
